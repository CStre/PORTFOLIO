export default interface Cat {
    id: string
    name : string,
    age : number,
    favoriteToy: string
}

export interface ICat {
    _id: string
    name : string,
    age : number,
    favoriteToy: string,
    __v: number
}